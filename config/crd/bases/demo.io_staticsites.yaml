---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: staticsites.demo.io
spec:
  group: demo.io
  names:
    kind: StaticSite
    listKind: StaticSiteList
    plural: staticsites
    singular: staticsite
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: combining all together
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: define specifications of the desired state of the static
              site
            properties:
              content:
                type: string
              domain:
                type: string
              replicas:
                format: int32
                type: integer
            required:
            - content
            - replicas
            type: object
          status:
            description: |-
              define the feedback structure
              Define the actual state of the static site
            properties:
              availableReplicas:
                format: int32
                type: integer
            required:
            - availableReplicas
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
